{
  "kind": "samples",
  "model": "erw",
  "params": {
    "p": 0.6
  },
  "scales": [
    4,
    8,
    16,
    32,
    64,
    128,
    256,
    512,
    1024,
    2048,
    4096
  ],
  "n": {
    "rule": "snr",
    "budget": 1000000000.0
  },
  "seed": null,
  "_note": [
    "DIFFUSIVE ARM, criterion E4. p = 0.6 < 3/4, so Bercu (2018) eq. (3.5) gives",
    "S_k / sqrt(k) -> N(0, 1/(3 - 4p)) and, with E S_k^2 = O(k) from eq. (A.3),",
    "E|S_k| / sqrt(k) -> sqrt(2 / (pi (3 - 4p))): gamma = 1/2, a0 = 1.030. Same",
    "gamma as the p = 1/2 control, but the memory now matters -- the walk is",
    "positively correlated -- so this is where the estimator must return 1/2",
    "through a correction it has not been told the shape of.",
    "",
    "Same ladder and budget as samples_calib_p0.5.json, deliberately: only p",
    "differs between the arms. See that recipe for why this ladder and these",
    "units."
  ]
}
