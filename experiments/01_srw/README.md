# 01_srw — Simple Random Walk

**Status: blocked on design.** The article's `appendix-SimpleRandomWalk` is currently an
empty section header — no closed-form $\E Y_i$, $\gamma$, or $\omega_1$ is written down
for this testbed yet. Needs discussion (which observable $Y_i$: return probability at
step $i$, range after $i$ steps, something else?) before any code is written. See
`PLAN.md` "Open questions before Phase 1".
