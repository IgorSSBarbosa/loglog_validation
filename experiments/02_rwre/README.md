# 02_rwre — Random Walk in Random Environment

**Status: not started**, depends on 01_srw being designed first (shares machinery).
Cross-check target: `../../critical_exponents/estimators/log_log_plot.py` implements
the same article estimator — worth comparing against once this rung exists, per
`CLAUDE.md`'s cross-project sync note. Do not import from `critical_exponents/`; this
project builds its own simulators from scratch (see `PLAN.md` ground rules).
