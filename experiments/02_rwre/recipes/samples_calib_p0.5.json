{
  "kind": "samples",
  "model": "rwre",
  "params": {
    "p": 0.5,
    "alpha": 0.5
  },
  "scales": [
    4,
    8,
    16,
    32,
    64,
    128,
    256,
    512,
    1024
  ],
  "n": {
    "rule": "snr",
    "budget": 1000000000.0
  },
  "seed": null,
  "_note": [
    "CALIBRATION ARM, criterion A2. At p = 1/2 the walker's drift is 1/2 whether",
    "it stands on a particle or a hole, so the environment is invisible and this",
    "recipe must reproduce experiments/01_srw's four exactly known constants:",
    "gamma = 1/2, a0 = sqrt(2/pi) = 0.797885, omega1 = 1, a1 = -1/4. It will not",
    "be bit-for-bit -- the environment consumes its own randomness -- so the",
    "test is distributional plus agreement of the fitted constants inside their",
    "standard errors. This is what catches a wrong update order, a drift, or a",
    "broken window boundary before any of it is blamed on physics.",
    "",
    "The \"snr\" rule with NO omega1 states no design constant at all: the",
    "uninformed default 0 makes n_i flat, which was measured on srw to be the",
    "best of five arms for identifying omega1 (see",
    "experiments/01_srw/recipes/samples_pilot.json). d is not stated either --",
    "models/rwre.py's cost_hint supplies it, and the pilot measures it from the",
    "clock and only scores the declaration against it.",
    "",
    "BUDGET UNITS. tools/allocation.py prices a sample at scale i as i**d with d",
    "from the model's cost_hint exponent -- NOT as cost_hint(i) itself, so the",
    "window_c = 12 prefactor is absent from the budget arithmetic and present in",
    "the wall clock. Measured on this machine (2026-09-15): one sample at every",
    "rung of 32..4096 costs 161 ms in total, so a flat replicate is n * 0.161 s.",
    "This budget is the PILOT's starting point; autopilot doubles it until both",
    "gates pass, and plan.py sizes the final run from what the pilot measured.",
    "",
    "LADDER 8..1024, NOT 32..4096 -- measured, 2026-09-15. A first pilot on",
    "32..4096 reported 'a pure power law still fits (chi2/dof = 0.77, p = 0.60):",
    "no correction visible yet for omega1 to be fitted to', and returned",
    "omega1 = 0.033 +/- 0.012 against srw's exact 1. That is the pilot being",
    "right: with a1 = -1/4 the correction to log E Y is a1/k = -0.8% at k = 32 and",
    "-0.006% at k = 4096, while the per-scale noise at that budget was 1.6%. The",
    "correction was below the noise at EVERY rung, so there was nothing to fit.",
    "",
    "Starting at k = 8 puts the correction at -3.1% on the bottom rung, above the",
    "noise, which is where experiments/01_srw measured omega1 = 1 in the first",
    "place (its own pilot ladder is 8..256). The ladder is also 8x cheaper per",
    "sample-column, so the same wall clock buys ~10x the samples. Powers of two",
    "throughout: a grid that mixes parities fits the |X_k| staircase as curvature",
    "and returns nonsense (tools/correction.py's CAUTION).",
    "",
    "Extended down to k = 4 on the pilot's own advice (2026-09-15): on 8..1024 it",
    "refused, reporting 'a pure power law still fits (chi2/dof = 2.67, p = 0.014):",
    "no correction visible' with the departure at the bottom rung 0.0116 against",
    "noise 0.0064, and said so in its own words -- 'more samples buy precision as",
    "sqrt(n); a smaller rung buys it as a power of the scale'. At k = 4 the",
    "one-correction truncation of eq. (232) still describes srw to 0.2% (exact",
    "E|S_4| = 1.5 against a0*2*exp(a1/4) = 1.4967), so the rung is inside the",
    "regime the model is allowed to be fitted on."
  ]
}
